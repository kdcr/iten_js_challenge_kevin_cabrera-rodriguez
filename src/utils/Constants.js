/**
 * The app color themes
 */
const AppThemes = {
  LIGHT: 'light',
  DARK: 'dark',
};

/**
 * Device sizes for responsiveness, string format with px appended
 */
const DeviceSizes = {
  sm: '640px',
  md: '768px',
  lg: '1024px',
  xl: '1280px',
  '2xl': '1536px',
};

/**
 * Device sizes for responsiveness, int number format
 */
const DeviceSizesNum = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
};

export { AppThemes, DeviceSizes, DeviceSizesNum };
